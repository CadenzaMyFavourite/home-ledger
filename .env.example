# HomeLedger does not require cloud credentials.
# Optional development logging for the Rust backend:
RUST_LOG=home_ledger=info,warn

# Local AI endpoints are configured in the app and should point to loopback
# addresses such as http://127.0.0.1:11434. Do not add secrets to this file.
